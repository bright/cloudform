import BackupPlan_ from './backupPlan';
import BackupVault_ from './backupVault';
import BackupSelection_ from './backupSelection';
export declare namespace Backup {
    const BackupPlan: typeof BackupPlan_;
    const BackupVault: typeof BackupVault_;
    const BackupSelection: typeof BackupSelection_;
    type BackupPlan = BackupPlan_;
    type BackupVault = BackupVault_;
    type BackupSelection = BackupSelection_;
}
